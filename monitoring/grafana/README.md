# Grafana

Grafana is provisioned with Prometheus and Loki datasources and the **MySQL HA
Overview** dashboard. It visualizes HA path selection, routing, WireGuard,
MySQL peer connectivity, latency, packet loss, replication, and HA events.
