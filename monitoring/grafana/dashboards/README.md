# Dashboards

The monitoring role provisions the **MySQL HA Overview** dashboard. Its panels
cover network-path and routing state, WireGuard, MySQL-to-MySQL connectivity,
latency, packet loss, replication, and structured HA events from Loki.
