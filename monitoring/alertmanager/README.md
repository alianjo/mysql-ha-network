# Alertmanager

Alertmanager groups alerts by alert name, host, and site and delivers email
notifications, including resolved alerts. SMTP credentials remain Vault-backed;
the receiver can be replaced by a webhook or paging integration.
